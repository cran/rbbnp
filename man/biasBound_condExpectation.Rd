% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{biasBound_condExpectation}
\alias{biasBound_condExpectation}
\title{Bias bound approach for conditional expectation estimation}
\usage{
biasBound_condExpectation(
  Y,
  X,
  x = NULL,
  h = NULL,
  h_method = "cv",
  alpha = 0.05,
  est_Ar = NULL,
  resol = 100,
  xi_lb = NULL,
  xi_ub = NULL,
  methods_get_xi = "snr",
  noise_floor = "auto",
  envelope_use_Y = TRUE,
  integer_r = TRUE,
  ora_Ar = NULL,
  kernel.fun = "Schennach2004",
  if_approx_kernel = TRUE,
  kernel.resol = 1000
)
}
\arguments{
\item{Y}{A numerical vector of sample data.}

\item{X}{A numerical vector of sample data.}

\item{x}{Optional. A scalar or range of points where the density is estimated. If NULL, a range is automatically generated.}

\item{h}{A scalar bandwidth parameter. If NULL, the bandwidth is automatically selected using the method specified in 'h_method'.}

\item{h_method}{Method for automatic bandwidth selection when h is NULL. Options are "cv" (cross-validation) and "silverman" (Silverman's rule of thumb). Default is "cv".}

\item{alpha}{Confidence level for intervals. Default is 0.05.}

\item{est_Ar}{Optional list of estimates for A and r. If NULL, they are computed using \code{get_est_Ar()}.}

\item{resol}{Resolution for the estimation range. Default is 100.}

\item{xi_lb}{Optional. Lower bound for the interval of Fourier Transform frequency xi. Used for determining the range over which A and r is estimated. If NULL, it is automatically determined based on the methods_get_xi.}

\item{xi_ub}{Optional. Upper bound for the interval of Fourier Transform frequency xi. Similar to xi_lb, it defines the upper range for A and r estimation. If NULL, the upper bound is determined based on the methods_get_xi.}

\item{methods_get_xi}{A string selecting the frequency-window rule used when xi_lb/xi_ub are NULL: "snr" (default; a signal-to-noise cutoff that selects a valid window at realistic sample sizes), "Schennach" (the data-driven rule of Schennach 2020, Theorem 2), or "Schennach_loose" (the initial, un-refined interval).}

\item{noise_floor}{Noise-floor form for the Schennach test: "auto" (default), "compact", or "general".}

\item{envelope_use_Y}{If TRUE (default), fit the regression envelope to the cross-spectrum \verb{|phi_YX|}; if FALSE, fit it to the marginal spectrum \verb{|phi_X|}.}

\item{integer_r}{If TRUE (default), clamp the fitted envelope slope up to r = 2 when it falls below the minimum smoothness assumed by Schennach (2020, Definition 2), i.e. r < 2, and refit A; this keeps the bias-bound integral finite. Slopes >= 2 are left unchanged.}

\item{ora_Ar}{Optional list of oracle values for A and r (for research/comparison purposes).}

\item{kernel.fun}{A string specifying the kernel function to be used. Options are "Schennach2004", "sinc", "normal", "epanechnikov".}

\item{if_approx_kernel}{Logical. If TRUE, uses approximations for the kernel function.}

\item{kernel.resol}{The resolution for kernel function approximation. See \code{\link{fun_approx}}.}
}
\value{
An object of class \code{bbnp_regression} with components:
\item{fitted_values}{\eqn{E[Y|X=x]} estimates (for range estimation)}
\item{x}{Evaluation points}
\item{estimate}{Point estimate (for single x)}
\item{conf_int}{List containing lower, upper bounds and conf_level. Note that
the confidence interval can be unbounded (i.e., contain \code{-Inf} or \code{Inf})
in regions where the estimated marginal density \eqn{\hat f(x)} is very close to zero,
because the estimator is formed as a ratio involving \eqn{1/\hat f(x)}.}
\item{bias_bound}{List containing b1x, byx, est_A, est_r, est_B, xi_interval}
\item{std_error}{Standard errors}
\item{marginal_density}{f(x) estimates}
\item{joint_density}{f_YX estimates}
\item{call}{The function call}
\item{bandwidth}{Bandwidth used}
\item{n}{Sample size}
\item{kernel}{Kernel type}
\item{data}{Original data (X, Y)}

Use \code{plot()}, \code{summary()}, \code{coef()}, \code{fitted()}, and \code{confint()} methods to work with the result.
}
\description{
Estimates the density at a given point or across a range, and provides visualization options for density,
bias, and confidence intervals.
}
\examples{
\donttest{
# Example 1: Point estimation at x = 1
X <- rnorm(100)
Y <- X^2 + rnorm(100)
fit <- biasBound_condExpectation(Y = Y, X = X, x = 1, h = 0.09)
print(fit)
fitted(fit)

# Example 2: Range estimation with plots
fit2 <- biasBound_condExpectation(Y = Y, X = X, h = NULL, h_method = "cv")
plot(fit2)              # Regression plot
plot(fit2, type = "ft") # Fourier transform plot
summary(fit2)
}
}
