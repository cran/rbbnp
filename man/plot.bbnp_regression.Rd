% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-plot.R
\name{plot.bbnp_regression}
\alias{plot.bbnp_regression}
\title{Plot Method for bbnp_regression Objects}
\usage{
\method{plot}{bbnp_regression}(
  x,
  type = c("regression", "ft"),
  fill_ci = .bbnp_pal[["ci"]],
  alpha_ci = 0.55,
  point_alpha = 0.28,
  point_color = .bbnp_pal[["point"]],
  ft_resol = 500,
  xi_range = NULL,
  expand = 2.5,
  ...
)
}
\arguments{
\item{x}{An object of class bbnp_regression}

\item{type}{Character string specifying plot type. Options are:
\itemize{
\item \code{"regression"} (default): Conditional expectation with confidence interval
\item \code{"ft"}: Fourier transform plot with estimated envelope
}}

\item{fill_ci}{Color for confidence interval ribbon (default: a muted blue).}

\item{alpha_ci}{Transparency for confidence interval ribbon (default: 0.35)}

\item{point_alpha}{Transparency for data points (default: 0.28)}

\item{point_color}{Color for data points (default: a soft grey).}

\item{ft_resol}{Resolution for Fourier transform plot (default: 500)}

\item{xi_range}{Optional numeric \code{c(lower, upper)} giving the frequency
range to \emph{display} in the \code{"ft"} plot. If \code{NULL} (default) a wide
range around the selected window is shown (see \code{expand}). This controls
only what is drawn; it does not change the fitting window \code{[xi_lb, xi_ub]}.}

\item{expand}{For the \code{"ft"} plot when \code{xi_range} is \code{NULL}: how
far past the selected window to display, as a multiple (default 2.5).}

\item{...}{Additional arguments (unused)}
}
\value{
A ggplot2 object
}
\description{
Creates visualizations of bias-bounded conditional expectation estimation results
}
\examples{
\donttest{
X <- rnorm(100)
Y <- X^2 + rnorm(100)
fit <- biasBound_condExpectation(Y, X, h = 0.1)
plot(fit)
plot(fit, type = "ft")
}
}
