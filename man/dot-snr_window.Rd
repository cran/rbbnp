% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.snr_window}
\alias{.snr_window}
\title{Signal-to-noise frequency-window selection rule}
\usage{
.snr_window(Y = 1, X, tau = NULL, run = 5L, per_log = 200)
}
\arguments{
\item{Y}{Numeric vector (or 1 for density).}

\item{X}{Numeric vector of sample data.}

\item{tau}{SNR threshold: \code{NULL} (default) uses \code{max(2, sqrt(2 * log(n)))}; a numeric
value, or a function of \code{n}. Letting \code{tau} grow faster than \verb{sqrt(log n)} preserves the
asymptotic rate condition of Schennach (2020) while keeping the rule usable in finite samples.}

\item{run}{Minimum consecutive below-threshold points for a sustained crossing.}

\item{per_log}{Grid resolution (points per log-unit of frequency).}
}
\value{
A list with \code{xi_lb} and \code{xi_ub}.
}
\description{
Selects the upper frequency cutoff as the point where the pointwise signal-to-noise ratio of
the empirical Fourier transform first drops (on a sustained run) below \code{tau}. Using the
pointwise (delta-method) standard error in place of Schennach (2020)'s worst-case uniform
error bound keeps the rule usable at realistic sample sizes, and a minimum-window guard
ensures it never returns an empty interval.
}
\keyword{internal}
