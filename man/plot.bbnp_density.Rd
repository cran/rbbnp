% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-plot.R
\name{plot.bbnp_density}
\alias{plot.bbnp_density}
\title{Plot Method for bbnp_density Objects}
\usage{
\method{plot}{bbnp_density}(
  x,
  type = c("density", "ft"),
  fill_ci = .bbnp_pal[["ci"]],
  fill_bias = .bbnp_pal[["bias"]],
  alpha_ci = 0.55,
  alpha_bias = 0.55,
  ft_resol = 500,
  xi_range = NULL,
  expand = 2.5,
  ...
)
}
\arguments{
\item{x}{An object of class bbnp_density}

\item{type}{Character string specifying plot type. Options are:
\itemize{
\item \code{"density"} (default): Density estimate with bias bounds and confidence intervals
\item \code{"ft"}: Fourier transform plot with estimated envelope
}}

\item{fill_ci}{Color for confidence interval ribbon (default: a muted blue).}

\item{fill_bias}{Color for bias bound ribbon (default: a muted terracotta).}

\item{alpha_ci}{Transparency for confidence interval ribbon (default: 0.30)}

\item{alpha_bias}{Transparency for bias bound ribbon (default: 0.45)}

\item{ft_resol}{Resolution for Fourier transform plot (default: 500)}

\item{xi_range}{Optional numeric \code{c(lower, upper)} giving the frequency
range to \emph{display} in the \code{"ft"} plot. If \code{NULL} (default) a wide
range around the selected window is shown (see \code{expand}). This controls
only what is drawn; it does not change the fitting window \code{[xi_lb, xi_ub]}.}

\item{expand}{For the \code{"ft"} plot when \code{xi_range} is \code{NULL}: how
far past the selected window to display, as a multiple (default 2.5).}

\item{...}{Additional arguments (unused)}
}
\value{
A ggplot2 object
}
\description{
Creates visualizations of bias-bounded density estimation results
}
\examples{
\donttest{
X <- rnorm(100)
fit <- biasBound_density(X, h = 0.1)
plot(fit)
plot(fit, type = "ft")
}
}
